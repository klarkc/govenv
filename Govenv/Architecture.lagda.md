# Architecture

This module holds repository architecture rules.

```agda
module Govenv.Architecture where

open import Agda.Builtin.Unit

ArchitectureValid : Set
ArchitectureValid = ⊤

architectureValid : ArchitectureValid
architectureValid = tt
```
